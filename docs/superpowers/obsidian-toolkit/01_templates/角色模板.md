---
uid: character-<% tp.date.now("YYYYMMDDHHmmss") %>
name: 
alias: []
faction: 
role: protagonist
status: alive
first-appearance: 
gender: 
age: 
occupation: 
location: 

relations:
  - target: 
    type: ally
    strength: strong
    start-chapter: 
    end-chapter: 
    note: 

attributes:
  personality: 
  ability: 
  weakness: 
  habit: 
  catchphrase: 

events:
  - chapter: 
    event: 首次出场
    significance: major

appearance:
  height: 
  build: 
  hair: 
  eyes: 
  distinguishing-features: 

backstory:
  origin: 
  family: 
  key-memory: 

character-arc:
  starting-point: 
  turning-point: 
  destination: 

plotlines: []

foreshadows: []

tags:
  - character
  - <% tp.frontmatter.faction %>
  - <% tp.frontmatter.role %>
---

# <% tp.frontmatter.name %>

> **阵营**：`<% tp.frontmatter.faction %>` | **角色定位**：`<% tp.frontmatter.role %>` | **状态**：`<% tp.frontmatter.status %>`

## 📋 基本信息

| 项目 | 内容 |
|------|------|
| **姓名** | <% tp.frontmatter.name %> |
| **别名** | <% tp.frontmatter.alias %> |
| **性别** | <% tp.frontmatter.gender %> |
| **年龄** | <% tp.frontmatter.age %> |
| **职业** | <% tp.frontmatter.occupation %> |
| **所在地** | <% tp.frontmatter.location %> |

## 🎭 外貌特征

- **身高**：<% tp.frontmatter.appearance.height %>
- **体型**：<% tp.frontmatter.appearance.build %>
- **发型**：<% tp.frontmatter.appearance.hair %>
- **眼睛**：<% tp.frontmatter.appearance.eyes %>
- **辨识特征**：<% tp.frontmatter.appearance["distinguishing-features"] %>

## 💭 性格特点

<% tp.frontmatter.attributes.personality %>

## ⚡ 核心能力

- **能力**：`<% tp.frontmatter.attributes.ability %>`
- **弱点**：`<% tp.frontmatter.attributes.weakness %>`
- **习惯**：`<% tp.frontmatter.attributes.habit %>`

> **口头禅**："<% tp.frontmatter.attributes.catchphrase %>"

## 🔗 关键关系

<%*
const relations = tp.frontmatter.relations || [];
let table = "| 角色 | 关系类型 | 强度 | 开始章节 | 备注 |\n";
table += "|------|---------|------|---------|------|\n";
for (const rel of relations) {
    table += `| ${rel.target || ""} | ${rel.type || ""} | ${rel.strength || ""} | ${rel["start-chapter"] || ""} | ${rel.note || ""} |\n`;
}
tR += table;
%>

## ⏰ 关键事件时间线

<%*
const events = tp.frontmatter.events || [];
let eventTable = "| 章节 | 事件 | 重要性 |\n";
eventTable += "|------|------|--------|\n";
for (const evt of events) {
    eventTable += `| ${evt.chapter || ""} | ${evt.event || ""} | ${evt.significance || ""} |\n`;
}
tR += eventTable;
%>

## 📖 角色弧光

### 起点
<% tp.frontmatter.character-arc["starting-point"] %>

### 转折点
<% tp.frontmatter.character-arc["turning-point"] %>

### 终点
<% tp.frontmatter.character-arc.destination %>

## 🏠 背景故事

### 出身
<% tp.frontmatter.backstory.origin %>

### 家人
<% tp.frontmatter.backstory.family %>

### 关键记忆
<% tp.frontmatter.backstory["key-memory"] %>

## 🔮 记忆点设计

> 这个角色的独特辨识点（不可与其他角色重复）

- **物品羁绊**：
- **创伤触发**：
- **习惯动作**：
- **口头禅**：

## 🎬 出场章节

- 第<% tp.frontmatter["first-appearance"] %>章：首次出场

## 📚 相关笔记

- [[]] - 
- [[]] - 

## 📝 备注
